# Publishes the Engrava MCP server entry to the official MCP Registry
# (https://registry.modelcontextprotocol.io) on every release tag.
#
# This publishes ONLY the registry entry (server.json). The PyPI package
# itself is published by the release flow (semantic-release); this workflow
# assumes `engrava==<tag>` is already on PyPI by the time the tag is pushed,
# because the registry validates that the referenced package version exists.
#
# Auth: DNS. The namespace ai.sovantica/* is verified by a TXT record on the
# apex of sovantica.ai; the Ed25519 private key is stored as the repo secret
# MCP_DNS_PRIVATE_KEY. The first listing is done manually by a maintainer
# (`mcp-publisher login dns` + `publish`); this automates re-publishing.
# NOTE: this workflow fails until MCP_DNS_PRIVATE_KEY is set in repo secrets.
name: Publish to MCP Registry

on:
  push:
    tags: ["v*"] # release tags cut by the release flow, e.g. v0.4.1

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read # DNS auth uses MCP_DNS_PRIVATE_KEY; no id-token needed

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher

      - name: Sync server.json version to the release tag
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          jq --arg v "$VERSION" '.version = $v | (.packages[] | .version) = $v' server.json > server.tmp
          mv server.tmp server.json

      - name: Authenticate to MCP Registry (DNS)
        run: ./mcp-publisher login dns --domain sovantica.ai --private-key "$MCP_DNS_PRIVATE_KEY"
        env:
          MCP_DNS_PRIVATE_KEY: ${{ secrets.MCP_DNS_PRIVATE_KEY }}

      - name: Publish server to MCP Registry
        run: ./mcp-publisher publish
